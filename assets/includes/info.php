<?php
$mysql_hostname = "localhost"; // Server name
$mysql_username = "root"; // Database user
$mysql_password = ""; // Database password
$mysql_database = "php-magazine"; // Database name


$domain = 'localhost'; // Web domain, eg. (localhost, IP or example.tld)
$subfolder = 'PHP-Magazine'; // You can use folder1 or folder1/folder2 ...
$site_url = "https://{$domain}/{$subfolder}"; // URL and subfolders of the website, eg. (http://${domain} or http//${domain}/subfolder). You can also use HTTP or HTTPS.
?>